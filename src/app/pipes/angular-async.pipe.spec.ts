import { AngularAsyncPipe } from './angular-async.pipe';

describe('AngularAsyncPipe', () => {
  it('create an instance', () => {
    const pipe = new AngularAsyncPipe();
    expect(pipe).toBeTruthy();
  });
});
